{
  "name": "n-file-system",
  "version": "1.0.0",
  "description": "基于 Node.js + Express + Multer + SQLite 的文件管理系统",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "dev": "nodemon src/app.js",
    "build:docker": "docker build -t n-file-system:latest . && docker save -o n-file-system.tar n-file-system:latest",
    "test": "node --test --test-concurrency=1",
    "storage:check": "node scripts/storage-check.js",
    "check": "node --check src/app.js && node --check src/db.js && node --check src/storage.js && node --check src/middleware/auth.js && node --check src/utils/security.js && node --check scripts/storage-check.js && node --check test/integration.test.js && node --check public/app.js"
  },
  "keywords": [
    "express",
    "multer",
    "sqlite",
    "file-upload"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "dotenv": "^17.4.2",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.0.2",
    "sqlite3": "^6.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  },
  "engines": {
    "node": ">=22"
  }
}
